exercicos wile dia 13/04
num = 0 
soma = 0

while num > -1 :
    num = float(input('adicione os numeros que você quiser e ao final aplicar -1 :'))
    soma = soma + num  
    
    if  num == -1 :
    
        soma = soma + 1
        print ('a soma dos numeros que voce colocou é de ',soma)
    
media = soma / num 
print('sua media é :',media)
-----------------------------------------------------------------------------------------------------------------------------------------
-----------------------------------------------------------------------------------------------------------------------------------------
-----------------------------------------------------------------------------------------------------------------------------------------
exercicio 2 wile 
for n in range (5): 
    soma = int(input('digite 5 numeros inteiros e positivos :'))
    
    par = soma / 2 
if par == int :
    print (f'você possui {par} de numeros pares.')


-----------------------------------------------------------------------------------------------------------------------------------------
-----------------------------------------------------------------------------------------------------------------------------------------
-----------------------------------------------------------------------------------------------------------------------------------------
exercicio 1 for
tab = float(input('digite a tabuada que deseja :'))
for i in range (10):
    print(tab,'x',i+1'=' ,tab*(i + 1))
